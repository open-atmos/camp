The auto-monarch code is the model that runs MONARCH with the autosubmit tool.

This folder is created because auto-monarch requires the platform in the name to allow running in different machines. 

The scripts here may only be a one line script running the corresponding script from the compile/ folder. This is just a matter of sintaxis for auto-monarch.

Also serves to know in which platforms CAMP is supported.